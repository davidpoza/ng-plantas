import { TranslateJournalTypePipe } from './translate-journal-type.pipe';

describe('TranslateJournalTypePipe', () => {
  it('create an instance', () => {
    const pipe = new TranslateJournalTypePipe();
    expect(pipe).toBeTruthy();
  });
});
